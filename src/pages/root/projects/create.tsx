import { CreateProjectForm } from '@project';

const CreateProject = () => {
  return (
    <div>
      <h1>Create Project</h1>
      <CreateProjectForm />
    </div>
  );
};

export default CreateProject;
